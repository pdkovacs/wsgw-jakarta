/**
 * Code whose effect lands at the app: the HTTP calls the gateway makes to it (relayed client
 * messages, disconnect notifications). Counterpart of {@link io.github.pdkovacs.wsgw.clientward}.
 */
package io.github.pdkovacs.wsgw.appward;
