# Test-only config: satisfies the required-but-default-less properties in AppConfig
# so the app boots under @QuarkusTest / @QuarkusIntegrationTest. Real deployments
# supply these via the environment; here we give harmless local values.
E2EAPP_PASSWORD_CREDENTIALS=[{"username":"test","password":"test"}]
E2EAPP_WSGW_HOST=localhost
E2EAPP_WSGW_PORT=8080
